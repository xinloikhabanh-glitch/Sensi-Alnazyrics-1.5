plugins {
    id 'com.android.application'
}

android {
    namespace 'com.sensi.analytics'
    compileSdk 34

    defaultConfig {
        applicationId "com.sensi.analytics"
        minSdk 26
        targetSdk 34
        versionCode 5
        versionName "1.5"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            // Ký bản release bằng debug keystore (khoá test có sẵn của Android SDK)
            // để máy thật/máy ảo cài đặt được trực tiếp. Đây KHÔNG phải khoá
            // dùng để phát hành lên Play Store - chỉ phục vụ mục đích test.
            signingConfig signingConfigs.debug
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    // Cần bật AIDL để build UserService dùng cho Shizuku
    buildFeatures {
        aidl true
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'androidx.cardview:cardview:1.0.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'

    // Thư viện Shizuku API - cho phép chạy lệnh shell qua quyền adb/root do Shizuku cấp
    implementation 'dev.rikka.shizuku:api:13.1.5'
    implementation 'dev.rikka.shizuku:provider:13.1.5'
}
